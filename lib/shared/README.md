# shared/

Componentes de presentación reutilizables entre features:

- `widgets/` — controles comunes (carátula, fila de pista, mini-reproductor,
  estados vacíos, indicadores de sync).
- `theme/` — tokens de diseño, paletas y tipografía. Puede importar la paleta
  activa del PC durante la sync (opt-in).
